import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Text("설정은 다음 단계에서 구현합니다.")
        }
        .padding(24)
        .frame(width: 480, height: 240)
    }
}
